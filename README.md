# Build Image only through shell scripts
1. chmod +x docker_build.sh 
2. ./docker_build.sh <Tag>

# Build Container only thorugh shell scripts
1. chmod +x docker_run.sh 
2. ./docker_run.sh <Tag> (defualt tag: latest)
